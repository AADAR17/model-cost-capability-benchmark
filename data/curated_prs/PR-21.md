# PR-21

- **Repo:** numpy/numpy
- **PR #:** 9998
- **URL:** https://github.com/numpy/numpy/pull/9998
- **Category:** Design/Feature
- **Title:** ENH: Nditer as context manager
- **Author:** mattip
- **Status:** Merged

## Description
Enables `nditer` to be used as a context manager (`with np.nditer(...) as it:`), eliminating manual `it = None` assignments to trigger writebacks. Adds `close()`/`NpyIter_Close` and deprecation warnings for the old non-context-manager pattern on writeable operands.

## Diff

### doc/release/1.15.0-notes.rst
```
+ Users of `nditer` should use the nditer object as a context manager
+ anytime one of the iterator operands is writeable, so that numpy can
+ manage writeback semantics, or should call `it.close()`. A
+ `RuntimeWarning` will be emitted otherwise in these cases.
+
+ `NpyIter_Close` has been added and should be called before
+ `NpyIter_Dealloc` to resolve possible writeback-enabled arrays.
```

### numpy/add_newdocs.py
```python
+ add_newdoc('numpy.core', 'nditer', ('close',
+ """
+ close()
+
+ Resolve all writeback semantics in writeable operands.
+ """))
```
Example updated:
```python
- for (a, b, c) in it:
+ with it:
+     for (a, b, c) in it:
```

### numpy/core/code_generators/numpy_api.py
```python
+ 'NpyIter_Close': (304,),
+ # End 1.15 API
```

### numpy/core/setup_common.py
```python
- C_API_VERSION = 0x0000000c
+ C_API_VERSION = 0x0000000d
```

### doc/source/reference/arrays.nditer.rst
```python
- >>> for x in np.nditer(a, op_flags=['readwrite']):
- ...     x[...] = 2 * x
+ >>> with np.nditer(a, op_flags=['readwrite']) as it:
+ ...     for x in it:
+ ...         x[...] = 2 * x
```

## Real Human Review Comments (ground truth)
- **(reviewer):** "Is it possible to create a simple custom context manager for `nested_iters`? Something like: class nested_iters(object): def __init__(self, args): self.mgr1 = nditer(args, 1)... def __enter__(self): return (self.mgr1.__enter__(), self.mgr2.__enter__())..."

## Fidelity note
High confidence — this PR's diff came through with consistent, specific detail across all files, including a C API version bump, which is a strong authenticity signal.
