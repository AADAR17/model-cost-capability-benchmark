# PR-18

- **Repo:** scikit-learn/scikit-learn
- **PR #:** 4294
- **URL:** https://github.com/scikit-learn/scikit-learn/pull/4294
- **Category:** Design/Feature (also a large Maintainability-style refactor)
- **Title:** [MRG+1] Make cross-validators data independent + Reorganize grid_search, cross_validation and learning_curve into model_selection
- **Author:** raghavrv
- **Status:** Merged

## Description
Consolidates `grid_search`, `cross_validation`, and `learning_curve` into a unified `model_selection` module, and makes cross-validators data-independent (no longer requiring data/data-dependent params at init).

## Diff (key excerpts)

### New module: sklearn/model_selection/
- `__init__.py`, `_split.py` (cross-validator implementations), `_search.py` (grid/random search), `_validation.py` (CV scoring functions)

### sklearn/cross_validation.py
```
+ warnings.warn("This module has been deprecated in favor of the "
+               "model_selection module into which all the refactored classes "
+               "and functions are moved. Also note that the interface of the "
+               "new CV iterators are different from that of this module. "
+               "This module will be removed in 0.19.", DeprecationWarning)
```

### sklearn/grid_search.py
```
+ warnings.warn("This module has been deprecated in favor of the "
+               "model_selection module into which all the refactored classes "
+               "and functions are moved. This module will be removed in 0.19.",
+               DeprecationWarning)
```

### sklearn/learning_curve.py
```
+ warnings.warn("This module has been deprecated in favor of the "
+               "model_selection module into which all the functions are moved."
+               " This module will be removed in 0.19",
+               DeprecationWarning)
```

### Codebase-wide import updates
```
- from sklearn.cross_validation import check_cv
+ from sklearn.model_selection import check_cv
- from sklearn.grid_search import GridSearchCV
+ from sklearn.model_selection import GridSearchCV
- from sklearn.cross_validation import train_test_split
+ from sklearn.model_selection import train_test_split
```

### API change
```
- for train, test in cv:
+ for train, test in cv.split(X, y):
```

### doc/whats_new.rst
New entries documenting the module reorganization and deprecations.

## Real Human Review Comments (ground truth)
- **jnothman:** "I'm -1 for moving cross_validation generators if they're getting a rewrite and we want to be able to use the same names."

## Fidelity note
Very large, multi-PR-merging reorganization. Diff above is a faithful summary of the structural change; not a complete line-by-line diff of every touched file (dozens across the codebase).
