# PR-19

- **Repo:** numpy/numpy
- **PR #:** 8247
- **URL:** https://github.com/numpy/numpy/pull/8247
- **Category:** Design/Feature
- **Title:** ENH: Add `__array_ufunc__`
- **Author:** charris
- **Status:** Merged

## Description
Introduces `__array_ufunc__`, the renamed and redesigned successor to `__numpy_ufunc__`, letting any class (ndarray subclass or not) override NumPy ufunc behavior.

## Diff

### doc/release/1.13.0-notes.rst
```
+ * Improved ability for classes to override default ufunc behavior. See
+   ``__array_ufunc__`` below.
+
+ ``__array_ufunc__`` added
+ -------------------------
+ This is the renamed and redesigned ``__numpy_ufunc__``. Any class, ndarray
+ subclass or not, can define this method or set it to ``None`` in order to
+ override the behavior of NumPy's ufuncs.
```

### doc/source/reference/arrays.classes.rst
```
- .. method:: class.__numpy_ufunc__(ufunc, method, i, inputs, **kwargs)
- .. versionadded:: 1.11
+ .. py:method:: class.__array_ufunc__(ufunc, method, *inputs, **kwargs)
+ .. versionadded:: 1.13

- *i* is the index of *self* in *inputs*.
- *inputs* is a tuple of the input arguments to the ``ufunc``
+ *inputs* is a tuple of the input arguments to the ``ufunc``.
+ If given, any ``out`` arguments, both positional and keyword, are
+ passed as a :obj:`tuple` in *kwargs*.
```

### numpy/core/_internal.py
```
+ def array_ufunc_errmsg_formatter(ufunc, method, *inputs, **kwargs):
+     """ Format the error message for when __array_ufunc__ gives up. """
+     args_string = ', '.join(['{!r}'.format(arg) for arg in inputs] +
+                             ['{}={!r}'.format(k, v)
+                              for k, v in kwargs.items()])
+     args = inputs + kwargs.get('out', ())
+     types_string = ', '.join(repr(type(arg).__name__) for arg in args)
+     return ('operand type(s) do not implement __array_ufunc__'
+             '({!r}, {!r}, {}): {}'
+             .format(ufunc, method, args_string, types_string))
```

### numpy/core/setup.py
```
+ join('src', 'private', 'ufunc_override.h'),
+ join('src', 'private', 'binop_override.h'),
+ join('src', 'private', 'ufunc_override.c'),
+ join('src', 'umath', 'override.c'),
+ join('src', 'umath', 'override.h'),
```
New source files added for the override-handling infrastructure.

## Real Human Review Comments (ground truth)
- **njsmith:** "I'm all for getting this into 1.13, but I don't get why we would want to enable it on master before it's ready."

## Fidelity note
Docs and setup/infra changes captured with confidence; the actual override dispatch logic in `override.c`/`ufunc_override.c` (the core mechanism) was not retrievable at line level.
