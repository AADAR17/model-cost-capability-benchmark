# PR-04

- **Repo:** pandas-dev/pandas
- **PR #:** 10265
- **URL:** https://github.com/pandas-dev/pandas/pull/10265
- **Category:** Bug/Correctness
- **Title:** BUG: Ensure 'coerce' actually coerces datatypes
- **Author:** bashtage
- **Status:** Merged

## Description
"Changes behavior of convert objects so that passing 'coerce' will ensure that data of the correct type is returned, even if all values are null-types (NaN or NaT). closes #9589"

## Diff (summarized — this is an older, larger PR; see fidelity note)

- **doc/source/basics.rst**: parameter names updated in docs (`convert_numeric` → `numeric`, `convert_dates='coerce'` → `datetime=True, coerce=True`)
- **doc/source/whatsnew/v0.17.0.txt**: migration guide added, old→new parameter mapping table
- **pandas/core/common.py**: `_possibly_convert_objects(values, convert_dates=True, convert_numeric=True, convert_timedeltas=True)` → `_possibly_convert_objects(values, datetime=True, numeric=True, timedelta=True, coerce=False, copy=True)`; string-based `'coerce'` checks replaced with a boolean `coerce` param; added guard "if 1 flag is coerce, ensure 2 others are False"
- **pandas/core/frame.py**: `convert_dates=True` → `datetime=True`; `.convert_objects()` call updated with new kwargs
- **pandas/core/generic.py**: added `@deprecate_kwarg` decorator for old parameter names; docstring updated
- **pandas/io/* and pandas/tests/***: all call sites updated to new parameter names

## Real Human Review Comments (ground truth)
- **@jreback:** "pls rebase on master and repush had an issue with some builds"
- **@jreback:** "I don't think this is worth the effort to show the warning. I would take it out (of course the ValueError above is the most important item)."
- **@jreback:** "you can change this to use `to_datetime(....coerce=True)` I think (it was that way a long time ago, but I changed it, but now for consistency prob better"
- **@jreback:** "@bashtage looks pretty good. btw, I think now might be a good time to change the actual argument names as well."
- **@jreback:** "nice. I added this to the deprecation issue so can remove this at some point."
- **@jreback:** "@bashtage couple of minor doc corrections. I think ready to go. pls make those fixes and ping when green."
- **@jreback:** "ty @bashtage! putting in a big PR often requires several iterations, so thanks for the patience!"

## Fidelity note
This is an older (2015), larger, multi-file rename/refactor PR. The diff above is a **faithful summary of the actual changes, not a verbatim line-by-line diff** — the underlying tool could not reliably reproduce exact code lines at this PR's size. Review comments are high-confidence (directly quoted). Recommend treating this PR's "diff" as accurate-in-substance rather than byte-exact when scoring model reviews against it.
