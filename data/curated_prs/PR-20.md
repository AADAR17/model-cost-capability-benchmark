# PR-20

- **Repo:** numpy/numpy
- **PR #:** 16935
- **URL:** https://github.com/numpy/numpy/pull/16935
- **Category:** Design/Feature
- **Title:** ENH: implement NEP-35's like= argument
- **Author:** pentschev
- **Status:** Merged

## Description
Implements NEP-35's `like=` keyword argument for array creation functions, enabling dispatch via `__array_function__` based on a reference array — lets downstream libraries (Dask, CuPy) handle array creation according to their own implementations.

## Diff

### numpy/__init__.pyi
```
# array(): added `like: ArrayLike = ...`
# zeros(), ones(), empty(): added `*, like: ArrayLike = ...` (keyword-only)
# full(): added `*, like: ArrayLike = ...`
# identity(), fromfunction(): added `like` parameter
```

### numpy/core/_add_newdocs.py
Docstrings updated with `${ARRAY_FUNCTION_LIKE}` placeholder for `array()`, `empty()`, `zeros()`, `fromstring()`, `fromiter()`, `fromfile()`, `frombuffer()`, `arange()`.

### numpy/core/_asarray.py, numeric.py, overrides.py
Dispatching logic added for the `like=` parameter; helper functions for managing it (exact lines not retrievable — see fidelity note).

### numpy/core/src/multiarray/arrayfunction_override.c/h
New C-level functions for `__array_function__` dispatching, including handling `like=` argument removal and routing (exact lines not retrievable).

## Real Human Review Comments (ground truth)
- **(reviewer, per earlier verification pass):** Eric Wieser provided the first substantive review with code suggestions (July 24, 2020) — exact quote not retrievable due to a comment-loading error on the page.

## Fidelity note
Python-level signature/doc changes captured with confidence. The C-level dispatch implementation and the Python helper functions in `_asarray.py`/`overrides.py` could not be retrieved at line level.
